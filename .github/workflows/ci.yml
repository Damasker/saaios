name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.97.1
        with:
          components: rustfmt, clippy
          targets: aarch64-unknown-linux-musl
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.13.0

      - name: fmt
        run: cargo fmt --all -- --check
      - name: clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Wayland headless vertical slice
        run: cargo test -p saai-displayd --all-targets -- --nocapture
      - name: test
        run: cargo test --workspace
      - name: Panther PID 1 and display watchdog
        run: >-
          cc -std=c11 -Wall -Wextra -Werror
          -Wno-unused-function -Wno-format-truncation
          os/targets/panther/tests/native-init-identity-test.c
          -o /tmp/native-init-identity-test &&
          /tmp/native-init-identity-test &&
          cc -std=c11 -Wall -Wextra -Werror
          os/targets/panther/src/display-supervisor.c
          -o /tmp/display-supervisor &&
          cc -std=c11 -Wall -Wextra -Werror
          os/targets/panther/tests/display-watchdog-test.c
          -o /tmp/display-watchdog-test &&
          /tmp/display-watchdog-test
      - name: e2e
        run: cargo test -p diagnose-slow-system -- --nocapture
      - name: Pixel 7 cross-build
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER: ${{ github.workspace }}/os/targets/panther/tools/zig-aarch64-musl.sh
          CC_aarch64_unknown_linux_musl: ${{ github.workspace }}/os/targets/panther/tools/zig-aarch64-musl.sh
          AR_aarch64_unknown_linux_musl: ${{ github.workspace }}/os/targets/panther/tools/zig-ar.sh
          CRATE_CC_NO_DEFAULTS: "1"
          RUSTFLAGS: -C link-self-contained=no
        run: >-
          zig cc -target aarch64-linux-musl -static -Os -s
          os/targets/panther/src/display-supervisor.c
          -o /tmp/saai-display-supervisor &&
          zig cc -target aarch64-linux-musl -static -Os -s
          os/targets/panther/src/native-init.c
          -o /tmp/saaios-native-init &&
          cargo build --profile pixel7 --locked
          --target aarch64-unknown-linux-musl
          -p saaios-runtime -p console-tui -p saai-displayd
