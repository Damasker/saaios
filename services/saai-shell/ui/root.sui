sui 2

screen root {
  component BottomNavigation {
    a11y = Button
    inset = safe
    tab now { loc = now }
    tab spaces { loc = spaces }
    tab search { loc = search }
    tab me { loc = me }
  }
}
