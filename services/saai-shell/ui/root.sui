sui 2

screen root {
  component BottomNavigation {
    a11y = Button
    inset = safe
    tab now { loc = now }
    tab inbox { loc = inbox }
    tab spaces { loc = spaces }
    tab me { loc = me }
  }
}
