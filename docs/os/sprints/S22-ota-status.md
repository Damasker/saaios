# Sprint 22 — OTA в интерфейсе

## Паспорт

- Состояние: `Done (частично, read-only)` (2026-09-13). Часть серии
  S14-S25 (см. S14). Физически подтверждено: карточка "Обновления"
  показывает верный слот (A -- совпадает с тем, что было прошито
  `init_boot_a`) и число попыток загрузки.
- Зависит от: S12 (`Done`, с честными оговорками).

## Goal

"Я" показывает реальный слот загрузки и счётчик попыток -- то, что
S12's инструменты уже персистентно пишут на диск.

## Scope

Входит: чтение `boot_slot`/`boot_attempts`, карточка "Обновления".
Не входит (честно, см. ADR-063): кнопка "проверить обновления сейчас"
-- нужен ещё не существующий URL-setting и фиксация пути OTA-бинарей.

## Test

Host: тесты/clippy/кросс-компиляция чисты. Device: подтверждено
физически 2026-09-13.

## Rollback

Предыдущий физически подтверждённый `saai-shell` (`cp && mv`).
