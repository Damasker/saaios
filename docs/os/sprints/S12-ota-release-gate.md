# Sprint 12 — OTA и release gate

## Паспорт

- Состояние: `Done` (2026-09-12, с честными оговорками -- см.
  Acceptance criteria). Change 1 -- ADR-044: механизм записи раздела
  выбран (переиспользование уже существующей `create_partition_node()`,
  allow-list `{init_boot_a, vendor_boot_a}` для записи). Change 2 --
  ADR-045: схема подписи (ed25519) и versioned manifest реализованы и
  протестированы, крейт `saai-ota-manifest`, 15/15 тестов + end-to-end
  на реальных файлах. Change 3 -- ADR-046: staged verified download
  физически подтверждён на реальном устройстве через USB-NCM
  (`saai-ota-stage`, `reqwest` вместо `busybox wget`) -- все 5
  сценариев (успех, чужая модель, downgrade, испорченная подпись,
  усечённая загрузка) подтверждены. Change 4 -- ADR-047: реальная
  запись в `init_boot_a`/`vendor_boot_a` физически подтверждена
  (self-write-back с согласия пользователя, backup хеш `vendor_boot_a`
  независимо совпал с уже задокументированным в panther/README.md) --
  две настоящие перезагрузки подряд, обе чистые, ноль дрейфа контента.
  Change 5 -- ADR-048: health confirmation + boot attempt limit +
  автоматический откат физически подтверждены через намеренно
  сломанную (заменён `saaios-runtime`) сборку, установленную реальным
  OTA-путём -- 3 плохие перезагрузки + автоматический откат + 1
  восстановительная перезагрузка, полное здоровье подтверждено
  независимой сверкой хеша. Тайминг-пробел `mark_current_slot_
  successful()` (найден ADR-046) остаётся открытым -- честно не
  устранён, задокументирован как известный, не блокирующий риск для
  будущего вшивания в `native-init.c`.
- Зависит от: S01-S11 (все `Done`). S04 -- узкий, но реальный
  precedent: `fastboot flash init_boot_a` + `--set-active=a` +
  `fastboot reboot` уже физически подтверждены надёжными несколько раз
  за проект (S04's Evidence, `product=panther`, `current-slot=a`,
  `unlocked=yes`) -- это precedent для "flash+reboot цикл работает",
  не precedent для настоящего двух-слотового SaaiOS OTA (такого
  прецедента в проекте нет вообще, см. Current state).
- Архитектурные решения: ADR-044 (Change 1) -- запись раздела строится
  поверх уже существующей, уже проверенной каждым боевым boot-циклом
  `create_partition_node()` (`native-init.c`), не нового
  GPT-парсера; жёсткий allow-list имён разделов -- единственный
  контроль безопасности write-пути. `services/saaios-runtime/src/
  ab_status.rs` признан нерелевантным реальным слотам этого устройства
  (уже отмечено в S11's Definition of Ready как решение, передаваемое
  сюда) -- не переиспользуется для slot-detection; реальный slot
  читается из `androidboot.slot_suffix` (`/proc/bootconfig`), уже
  корректно читается `crates/system-tools::boot_slot_from`.
- Рабочий fallback: пока Change'и этого спринта не приняты или
  откатываются, единственный путь обновления остаётся сегодняшним --
  ручной `fastboot flash` с хоста в bootloader-режиме, физически
  проверенный весь проект (S01-S11).

## Goal

Новый стек можно безопасно и проверяемо обновить на тестовом Pixel 7:
подписанный manifest и artifact, staged verified download, запись без
риска для активного загружаемого слота, ограниченное число boot
attempts, health confirmation и автоматический откат на последний
физически подтверждённый рабочий образ -- всё физически проверено на
реальном устройстве, а не предположено. Переход от сохранённого
Android в слоте B к второму SaaiOS-слоту и настоящий cross-slot A/B
hardware failover -- явно вне скоупа этого Definition of Ready (см.
Scope).

## Current state

Проверено на реальном устройстве и по коду перед написанием этого
Definition of Ready (не предполагается).

**Единственный реально доступный SaaiOS-слот -- A; слот B закрыт
политикой.** Подтверждено `docs/os/targets/panther/README.md`: SaaiOS
установлена и проверена только в слоте A (`init_boot_a` +
`vendor_boot_a`), слот A отмечен загрузчиком успешным. Слот B хранит
штатный Android (`CP2A.260705.006`) как аварийный возврат и явно
закрыт для использования SaaiOS до отдельного, специально
подтверждённого этапа после готовности внешнего factory recovery,
которого сегодня не существует. Следствие: настоящее аппаратное A/B
двух-SaaiOS-слотовое переключение физически невозможно проверить в
рамках этого документа без нарушения уже принятой owner policy --
Acceptance criteria этого документа сформулированы вокруг единственного
доступного слота, не двух.

**Исправлено ADR-044: в SaaiOS уже есть путь писать раздел изнутри
себя, только для одного конкретного раздела -- он уже физически
проверен каждой загрузкой.** Первоначальная версия этого раздела (до
ADR-044) утверждала, что такого пути нет вовсе -- вывод был сделан по
отсутствию `/dev/block/by-name/` (подтверждено: `ls: No such file or
directory`, native PID1 не создаёт GPT-based символьные ссылки, в
отличие от штатного Android `init`/`fs_mgr`) и по `grep` на ключевые
слова `ota/OTA/A-B/bootctl/slot`, не на конкретные имена функций.
ADR-044's полная ревизия `native-init.c` нашла: (а) `/sys/block/
<disk>/<partition>/uevent` уже отдаёт `PARTNAME=`/`MAJOR=`/`MINOR=`
для всех 55 реальных разделов -- ядро уже парсит GPT, свой парсер не
нужен; (б) `create_partition_node()` (`native-init.c:339`) -- готовая,
уже работающая функция, резолвящая раздел по `PARTNAME` и делающая
`mknod` -- уже вызывается каждую загрузку для `persist` и `devinfo`;
(в) `mark_current_slot_successful()` (`:478`) уже реально **пишет**
один байт в раздел `devinfo` (`O_RDWR`, `pwrite`, `fsync`) на каждой
загрузке -- единственное такое место в файле, но оно уже есть и уже
проверено всей историей проекта. OTA's механизм записи (Change 1,
ADR-044) строится поверх этого прецедента, не изобретает новый; полная
карта разделов и находки -- в ADR-044. Все образы за весь проект
(S01-S11) тем не менее по-прежнему устанавливались только через
`fastboot flash`/`--set-active` с хоста -- существующий write-путь
использовался только для одного метаданного-бита, не для содержимого
загрузочных образов.

**Реальный `boot_slot` уже физически подтверждён читаемым.**
`androidboot.slot_suffix = "_a"` подтверждён напрямую в `/proc/
bootconfig` этого запущенного устройства этой сессией (не в `/proc/
cmdline`, где параметра нет) -- совпадает с тем, что `crates/
system-tools/src/lib.rs::boot_slot_from` уже проверяет оба источника.
Это единственный уже интегрированный, живой источник A/B-состояния.

**`ab_status.rs` -- нерелевантный, неподключённый код.**
`services/saaios-runtime/src/ab_status.rs` реализует статус-модуль над
вымышленной директорией `/var/lib/saaios/ab` (`A`/`B`, `current`-
симлинк, `BOOT_OK`-маркер) -- никак не связан с реальным
bootloader-уровневым механизмом этого устройства; на телефоне
`SAAIOS_AB_ROOT` не задан, `read_ab_status` возвращает `enabled:
false`. Решение (уже намеченное в S11's DoR, подтверждается здесь):
признаётся нерелевантным реальным слотам, не адаптируется -- OTA
использует `androidboot.slot_suffix`/GPT-факты напрямую, не этот
модуль.

**Исправлено ADR-045: схема подписи и manifest теперь реализованы.**
На момент написания этого DoR в проекте не было ни одной крипто-подписи
(`grep` по всем `Cargo.toml` на `ed25519`/`dalek`/`ring =`/`rsa =`/
`Signature` -- ноль совпадений, манифесты приложений S05/S07 явно не
подписаны). Change 2 (ADR-045) закрыл этот пробел: новый крейт
`crates/saai-ota-manifest` -- `ManifestBody` (versioned,
`deny_unknown_fields`), подпись ed25519, anti-downgrade в две
проверки (строго новее установленной версии + не ниже
`min_supported_version`), allow-list разделов как параметр вызова
(ADR-044's `{init_boot_a, vendor_boot_a}` передаётся снаружи). CLI
`saai-ota-sign` (keygen/build/verify) -- build-server-side инструмент,
никогда не запускается на устройстве.

**Download-клиент уже есть, свободное место для staging достаточно.**
`busybox` на устройстве включает applet `wget` (подтверждено `busybox
2>&1 | grep -i wget` на реальном телефоне). `/data` (`saaios-data`,
F2FS) -- 109.9 ГБ, занято ~1 ГБ, 108.9 ГБ свободно -- достаточно для
staged-загрузки любого реалистичного полного системного artifact.
`/metadata` -- всего 62 МБ, занято 70% (18.5 МБ свободно) -- слишком
мал для staging, годится только под маленькие boot/update-state
файлы, как и сегодня.

**Существующая дисциплина хешей -- задел, не замена подписи.**
`DEVELOPMENT_PROCESS.md` уже требует SHA-256 + commit для каждого
установленного образа (используется весь проект, S01-S11) -- это
integrity/provenance-запись, а не аутентификация; сама по себе не
защищает от подмены artifact на пути доставки, только позволяет
заметить несовпадение после факта. Подпись (Change 2) добавляет именно
это недостающее свойство.

**Найден реальный тайминг-пробел вокруг существующей
`mark_current_slot_successful()` (ADR-044, для Change 5, не решается
здесь).** Функция вызывается сразу после `"native userspace ready"` --
задолго до подтверждения здоровья UI-слота, раньше, чем мог бы
проявиться класс отказа, найденный S11/ADR-043 (OOM `saai-displayd`,
падение в `drm-splash`). Если бит в `devinfo`, который она выставляет,
-- то, что реальный ABL-бутлоадер читает для собственного
tries-remaining/successful A/B-учёта, текущий тайминг уже глушит
аппаратный автооткат именно для этого класса отказов. Проверяется ли
бит бутлоадером вообще -- открытый, физически проверяемый вопрос для
Change 5.

## Scope

- входит:
  - Change 1 (спайк, обязателен первым) -- **закрыт, ADR-044**:
    механизм записи раздела выбран -- переиспользование уже
    существующей, уже проверенной каждой загрузкой
    `create_partition_node()` (`native-init.c`), без нового
    GPT-парсера; запись ограничена жёстким allow-list имён разделов,
    сегодня ровно `{init_boot_a, vendor_boot_a}`. Спайк был чисто
    read-only (только `/sys/class/block/*/uevent` и просмотр
    исходников) -- ничего не записано на этом шаге;
  - Change 2 (полностью host-testable) -- **закрыт, ADR-045**: ed25519
    подпись, `ManifestBody` (device model/hardware id, версия,
    min-version для anti-downgrade, список разделов с SHA-256 и
    размером), подпись поверх всего. Крейт `crates/saai-ota-manifest` +
    CLI `saai-ota-sign` (keygen/build/verify). 15/15 unit-тестов +
    end-to-end на реальных файлах на R620 -- ни одного изменения на
    телефоне;
  - Change 3 -- **закрыт, ADR-046**: staged verified download на
    реальном устройстве через USB-NCM в `/data/saaios/update-staging`
    (сервис `saai-ota-stage`, `reqwest` вместо изначально
    предполагавшегося `busybox wget` -- уже проверенный на этом
    таргете HTTP-клиент, см. ADR-046). Полная проверка манифеста
    (подпись/схема/модель/anti-downgrade/allow-list) до скачивания
    любого раздела; потоковое хеширование каждого раздела,
    атомарный `rename()` только при точном совпадении sha256/размера.
    Все 5 негативных/позитивных сценариев подтверждены физически:
    валидный manifest+artifact, чужая модель, downgrade, испорченная
    подпись, усечённая (прерванная) загрузка -- каждый явно
    отвергается, ничего не пишется мимо staging-каталога;
  - Change 4 -- **закрыт, ADR-047**: реальная запись `init_boot_a`/
    `vendor_boot_a` (сервис `saai-ota-write`, backup+write+verify+
    auto-restore-on-failure, поверх ADR-044's `create_partition_node()`-
    резолюции). Физически подтверждено с явного согласия пользователя:
    self-write-back (контент не меняется, минимизация риска) на обоих
    разделах, каждый -- с настоящим cold reboot сразу после. Честно
    ограничена единственным доступным SaaiOS-слотом (слот B закрыт
    политикой, см. Current state) -- safe in-place update с backup/
    restore, не cross-slot A/B failover;
  - Change 5 -- **закрыт, ADR-048**: health confirmation (process-
    presence проверка core-сервисов с бюджетом опроса) + персистентный
    счётчик boot attempts + автоматический откат при провале
    health-check, физически продемонстрирован через намеренно
    нездоровую тестовую сборку (реальная копия `init_boot_a` с
    `saaios-runtime`, заменённым на завершающуюся ошибкой заглушку),
    установленную реальным OTA-путём (Changes 2-4). Ни один кусок не
    вшит в `native-init.c` -- `record-attempt`/`check` вызывались
    вручную, имитируя будущую автоматическую интеграцию.
- не входит: переход от сохранённого Android в слоте B к второму
  SaaiOS-слоту (отдельный, специально подтверждённый этап после
  готовности внешнего factory recovery -- ни один из них не существует
  сегодня); настоящий cross-slot A/B hardware failover (недостижим без
  входа выше); UI для запуска/показа прогресса обновления (может
  понадобиться позже отдельным изменением, не блокирует эту сервисную
  инфраструктуру); доставка обновления по сети, отличной от уже
  проверенного USB-NCM (Wi-Fi-доставка -- будущая работа).

## Change

1. Спайк: механизм записи раздела -- **закрыт (ADR-044)**:
   переиспользование `create_partition_node()`, allow-list
   `{init_boot_a, vendor_boot_a}`.
2. Подпись + versioned manifest -- **закрыт (ADR-045)**: крейт
   `saai-ota-manifest`, ed25519, 15/15 тестов + end-to-end на реальных
   файлах, полностью host-testable.
3. Staged verified download на устройстве в `/data` -- **закрыт
   (ADR-046)**: `saai-ota-stage`, все 5 сценариев физически
   подтверждены через USB-NCM, никаких изменений реальных разделов.
4. Запись обновления в единственный доступный SaaiOS-слот (A) с
   backup/restore как rollback-путём -- **закрыт (ADR-047)**: реальная
   запись обоих разделов физически подтверждена, две настоящие
   перезагрузки подряд чистые, честно не cross-slot failover.
5. Health confirmation + boot attempt limit + автоматический откат --
   **закрыт (ADR-048)**: 3 плохие перезагрузки + автоматический откат
   + 1 восстановительная перезагрузка, реальная нездоровая сборка
   через настоящий OTA-путь.

## Test

- host: Change 2 полностью юнит/интеграционно тестируется на R620 --
  корректная/испорченная подпись, повреждённый artifact, чужая модель,
  downgrade, всё без телефона;
- protocol/schema: manifest -- versioned, отклонение неизвестной схемы
  (тот же принцип, что S05's manifest);
- device smoke: Change 3's download в staging, Change 4's запись,
  Change 5's health-check -- все требуют реального Pixel 7;
- fault injection: прерванное соединение при download (физически
  отключить USB на середине), повреждённый artifact (побитый байт
  после подписи), намеренно нездоровая сборка для health-check drill;
- cold reboot: обязателен после Change 4 -- подтвердить, что
  обновлённый образ переживает настоящую перезагрузку, не только
  runtime-подмену (тот же принцип, что весь S01-S11);
- physical acceptance: не применимо напрямую в этом спринте -- нет
  нового пользовательского жеста/цвета/звука; будущий UI прогресса
  обновления вне скоупа.

## Acceptance criteria

- манифест и artifact без валидной подписи отвергаются до какой-либо
  записи раздела -- **подтверждено** (ADR-045 unit-тесты + ADR-046
  реальный отказ на устройстве до создания staging-каталога);
- запрещённый downgrade и artifact для другой модели устройства
  отвергаются -- **подтверждено** физически (ADR-046: оба сценария
  реально отклонены на устройстве до staging);
- прерванная или повреждённая staged-загрузка не доходит до записи
  раздела -- **подтверждено** физически (ADR-046: усечённая загрузка
  отклонена с точным несовпадением размера/хеша, `.tmp` удалён);
- запись обновления не портит активный загружаемый слот при отказе на
  любом шаге -- **подтверждено** через backup/restore drill (ADR-047:
  self-write-back + 2 реальные перезагрузки; ADR-048: реальный
  автоматический откат после 3 плохих загрузок + восстановительная
  перезагрузка) -- не через cross-slot failover, см. Scope;
- после обновления намеренно нездоровой тестовой сборкой срабатывает
  ограниченный boot attempt limit и автоматический откат на последний
  физически подтверждённый рабочий образ -- **подтверждено** физически
  (ADR-048: реальная сборка со сломанным `saaios-runtime`, 3 бада,
  автоматический откат, независимая сверка хеша после восстановления);
- обновлённый образ переживает настоящий cold reboot -- **подтверждено**
  (ADR-047: дважды на self-write-back; ADR-048: 4 раза подряд в
  drill'е, включая финальную восстановительную загрузку);
- честно зафиксировано и не выдаётся за большее: переход слота B и
  настоящий cross-slot A/B hardware failover не проверялись и не
  входили в этот спринт; health-check -- process-presence, не
  протокольный round-trip в каждый сервис; тайминг-пробел
  `mark_current_slot_successful()` (ADR-046) остаётся открытым --
  boot.log показал "slot A marked successful" даже когда
  `saaios-runtime` не поднялся, и это не исправлено в рамках S12;
  ничего не вшито в `native-init.c` -- вся автоматика Change 5
  вызывалась вручную, имитируя будущую интеграцию.

## Threat / privacy impact

- новая полномочная поверхность: компонент, способный писать в
  загрузочный раздел -- максимальный blast radius в проекте на
  сегодня; каждый шаг Change 1 и Change 4 требует явного согласия
  пользователя перед первой реальной попыткой, тем же паттерном, что
  S11's suspend-спайк;
- приватный ключ подписи никогда не хранится на устройстве и не
  коммитится в git -- только публичный ключ верификации встраивается
  в образ; сам приватный ключ -- на R620 либо отдельном офлайн-
  хранилище, вне скоупа кода этого спринта, но явно фиксируется как
  требование до первого реального подписанного релиза;
- staged download использует уже изолированный USB-NCM-канал (S01,
  `saaios-runtime` слушает только на USB-интерфейсе) -- не открывает
  новую сетевую поверхность;
- телеметрия обновления (что скачано, когда, успех/откат) -- без
  пользовательского контента, тем же принципом, что уже действующий
  audit log (S10).

## Rollback

- до Change 4: ни один Change не меняет ни один установленный раздел
  устройства -- откат не требуется, сегодняшний `fastboot`-путь
  остаётся рабочим без изменений;
- после Change 4/5: последний физически подтверждённый рабочий образ
  слота A, сохранённый как backup до записи; ручной `fastboot flash` с
  хоста остаётся рабочим fallback независимо от состояния нового кода.

## Evidence

**Change 1 закрыт (ADR-044), 2026-09-12.** Read-only спайк на реальном
устройстве: полная карта 55 разделов на 4 дисках снята через
`/sys/block/*/*/uevent` (`PARTNAME=`); найден и задокументирован уже
существующий, уже боевой механизм записи раздела
(`create_partition_node()` + `mark_current_slot_successful()` в
`native-init.c`), что потребовало honest-исправления Current state
этого документа (см. выше) -- изначальная формулировка "нет ни одного
пути писать раздел" была неполной. Побочная находка для Change 5:
реальный тайминг-пробел вокруг `mark_current_slot_successful()`,
вызываемой до подтверждения здоровья UI, потенциально глушащий
аппаратный автооткат. Ничего не записано на устройство в рамках этого
Change.

**Change 2 закрыт (ADR-045), 2026-09-12.** Крейт `crates/
saai-ota-manifest`: ed25519-подпись, versioned `ManifestBody`
(`deny_unknown_fields`), anti-downgrade в две проверки, allow-list
как параметр. `cargo test -p saai-ota-manifest` -- 15/15 зелёные
(валидный манифест, испорченная подпись, подделанное тело, чужой ключ,
чужая модель устройства, downgrade, повторная установка той же
версии, ниже min_supported_version, раздел не из allow-list,
неизвестное поле схемы, неподдерживаемая схема, повреждённый artifact,
целый artifact, отсутствующий в манифесте раздел, стабильность
канонических байт). End-to-end на реальных файлах в `/tmp/ota-e2e` на
R620 (`saai-ota-sign keygen`/`build`/`verify`) -- те же пять сценариев
физически подтверждены вне юнит-тестов. `cargo clippy --all-targets`
чисто, `cargo fmt -- --check` чисто, `cargo build --workspace` чисто.
Ничего не менялось и не проверялось на телефоне -- Change 2 полностью
host-only, как и требовал DoR.

**Change 3 закрыт (ADR-046), 2026-09-12.** Новый одноразовый сервис
`services/saai-ota-stage` (тот же "доказать перед вшиванием в init"
паттерн, что ADR-030 для `saai-taskd`), кросс-компилирован
`aarch64-unknown-linux-musl`/`pixel7` (`7377228b...`), реально
прошит на устройство и физически прогнан через USB-NCM (throwaway
HTTP-сервер `172.31.7.2:8877`, тот же паттерн, что S11). Все 5
сценариев подтверждены на реальном устройстве: валидный
manifest+artifact (2 раздела, 2097152 и 1048576 байт, hash сверен
независимо через `busybox sha256sum`); чужая модель устройства и
downgrade и испорченная подпись -- все три отклонены ДО создания
staging-каталога; усечённая (симулирующая обрыв) загрузка одного
раздела -- отклонена с точным несовпадением размера/хеша, `.tmp`
удалён, второй раздел при этом успешно застейджен независимо.
Библиотека `saai-ota-manifest` расширена `verify_partition_digest`
(потоковая проверка без буферизации всего файла), 17/17 тестов,
`clippy`/`fmt`/полная сборка workspace чисты. Устройство после раунда
здорово (uptime вырос монотонно, память стабильна, ни одного падения
core-сервисов); тестовый staging-каталог удалён, deploy-сервер и
firewall-правило убраны. Известное ограничение: реализована
per-partition проверка и отказ, но не настоящая byte-range
resumability -- честно не заявляется как выполненное.

**Change 4 закрыт (ADR-047), 2026-09-12.** Новый одноразовый сервис
`services/saai-ota-write`, кросс-компилирован (`cf58e987...`), 7/7
host-тестов (включая автоматическое восстановление при отказе записи
на хосте, где процесс не root -- на устройстве та же индукция отказа
через `chmod` не работает, так как весь процесс root, честно
зафиксированная асимметрия среды). Безопасная часть на устройстве:
`install` дважды прогнан через `--unsafe-test-target-override` на
scratch-файлы, ноль касаний реальных разделов, хеши независимо сверены.

**Реальная запись, с явным согласием пользователя перед первой
попыткой.** Backup `vendor_boot_a` (67108864 байт) дал хеш
`6e7be35d...`, **точно совпавший** с уже задокументированным в
`panther/README.md` SHA-256 оригинального образа -- независимое
подтверждение корректности до единой записи. Self-write-back (контент
не меняется) на `vendor_boot_a`, затем настоящий `reboot -f` -- чистая
загрузка (`uptime` 21.07с, все сервисы штатно, `slot A marked
successful` в `boot.log`). То же самое на `init_boot_a` (8388608 байт,
свежий backup непосредственно перед записью совпал байт-в-байт с
предыдущим) -- вторая настоящая перезагрузка подряд, снова чистая
(`uptime` 19.56с). Финальная независимая проверка после обеих
перезагрузок: оба раздела перечитаны заново, хеши идентичны исходным
байт-в-байт -- ноль дрейфа за весь цикл. Тестовые артефакты, deploy-
сервер и firewall-правило убраны; `/data/saaios/system/backup/
{init_boot_a,vendor_boot_a}.img.bak` оставлены как канонический задел
для Change 5.

Честно зафиксировано: генуинно другой контент на реальный раздел не
писался (гарантированно сломал бы загрузку со случайными Change 2/3
фикстурами) -- откладывается до Change 5, где уже запланирована
намеренно нездоровая тестовая сборка для health-check drill.

**Change 5 закрыт (ADR-048), 2026-09-12 -- S12 полностью закрыт.**
Новый одноразовый сервис `services/saai-ota-health` (`counter.rs` +
`health.rs`, 8/8 host-тестов, кросс-компиляция чиста
`c355290a...`). Намеренно нездоровая тестовая сборка -- не синтетика:
точная копия реально прошитого `init_boot_a`
(`dist/panther/stale-saaios-panther-init_boot.img.20260911T1358Z`,
байт-в-байт совпадает с ADR-047's backup), у которой `magiskboot`
заменил `saaios-runtime` (3.26 МБ) на тривиальную заглушку с `exit 1`
(3.94 КБ) -- собрана, подписана (`saai-ota-sign`, версия 0.15.0) и
установлена через настоящий, уже проверенный OTA-путь (`saai-ota-stage`
+ `saai-ota-write install`), не подсунута мимо пайплайна.

Три реальные перезагрузки подряд с этой сборкой: каждый раз устройство
поднималось полностью штатно (Wi-Fi, Bluetooth, UI, `native userspace
ready`), но `ps` подтверждал отсутствие `saaios-runtime` (4 из 5
core-процессов); `saai-ota-health check` каждый раз честно вернул
`UNHEALTHY ... missing: saaios-runtime` после полного бюджета опроса
(8с). На третьей попытке (`--limit 3`) `maybe-rollback` вызвал уже
проверенный `saai-ota-write restore`, восстановив исходный образ
байт-в-байт; четвёртая, восстановительная перезагрузка вернула ВСЕ 5
core-сервисов, `check` подтвердил `healthy`, независимая сверка
(`saai-ota-write backup` в throwaway-файл + `sha256sum`) совпала с
исходным хорошим хешем точно.

Честно зафиксировано, не устранено в рамках S12: тайминг-пробел
`mark_current_slot_successful()` (найден ADR-046) -- `boot.log`
показал `slot A marked successful` во всех трёх плохих загрузках,
несмотря на отсутствие `saaios-runtime`; ничего из Change 5's
автоматики не вшито в `native-init.c` -- `record-attempt`/`check`
вызывались вручную по serial. Устройство после всего раунда чистое:
`installed-version` возвращён к `0.10.0`, тестовые staging/counter-файлы
удалены, deploy-сервер и firewall-правило убраны, канонические
backup-файлы обоих разделов остаются на устройстве.

**Итог S12**: все 5 Change'ей закрыты (ADR-044..048), полный
подписанный OTA-цикл (manifest -> staged download -> запись ->
health-check -> автоматический откат) физически подтверждён на
реальном Pixel 7 сквозным реальным прогоном, включая настоящую
нездоровую сборку и настоящее восстановление. Честные ограничения:
единственный доступный SaaiOS-слот (cross-slot A/B failover не
проверялся, слот B закрыт политикой), health-check -- process-presence,
не протокольный, ничего не вшито в boot-critical путь `native-init.c`.

Заполняется по мере закрытия каждого Change -- отдельный ADR на
каждый Change, тем же паттерном, что S09/S10/S11.
