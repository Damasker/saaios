# Sprint 15 — Хранилище

## Паспорт

- Состояние: `Done` (2026-09-13). Часть серии S14-S25 -- физическая
  проверка выполнена в финальном консолидированном проходе (см.
  S14's паспорт).
- Зависит от: S13 (`Done`).

## Goal

"Я" показывает реальное занятое/свободное место на `/data`.

## Scope

Входит: чтение через `df -k /data`, карточка "Хранилище".
Не входит: разбивка по приложениям, очистка кэша.

## Test

Host: тесты/clippy/кросс-компиляция чисты. Device: подтверждено
физически 2026-09-13 -- карточка "Хранилище" показывает разумный
размер.

## Rollback

Предыдущий физически подтверждённый `saai-shell` (`cp && mv`).
