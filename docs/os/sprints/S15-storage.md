# Sprint 15 — Хранилище

## Паспорт

- Состояние: `Реализовано, проверка отложена` (2026-09-13). Часть серии S14-S25 --
  физическая проверка отложена на общий финальный шаг (см. S14's
  паспорт для полного объяснения).
- Зависит от: S13 (`Done`).

## Goal

"Я" показывает реальное занятое/свободное место на `/data`.

## Scope

Входит: чтение через `df -k /data`, карточка "Хранилище".
Не входит: разбивка по приложениям, очистка кэша.

## Test

Host: тесты/clippy/кросс-компиляция чисты. Device: отложено.

## Rollback

Предыдущий физически подтверждённый `saai-shell` (`cp && mv`).
