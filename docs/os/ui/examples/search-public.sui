sui 2
screen search {
  component ContextHeader {
    text = Title
    a11y = Heading
    loc = search.header
    focus = 0
    inset = safe
  }
  component DataRow {
    text = Body
    a11y = Button
    loc = search.item
  }
  component BottomNavigation {
    a11y = Button
    focus = 1
    scroll = none
    inset = safe
    tab now { loc = now }
    tab spaces { loc = spaces }
    tab search { loc = search }
    tab me { loc = me }
  }
}
